没有允许禁止安装任何依赖

当前项目是在mac上开发，linux上运行。mac只需要确认链路是通的

写代码要考虑可维护性，不要全都代码都堆砌到一个文件中

减少没必要的改动。

文件之间的函数调用要保存谨慎，不要出现一个模块（文件夹)调另一个同层级模块的子模块的函数的情况，否则会出现各种奇怪的耦合。除非是某些特殊模块，比如记录器，或已经是深度耦合的模块间调用

下层模块不能够反向依赖上层模块。
