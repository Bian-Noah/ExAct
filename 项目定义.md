# 项目定义文档

## 项目名称

**基于探索-执行双阶段的仿真机械臂具身操控智能体**

## 一、项目要干什么

在 PyBullet 仿真环境中，构建一个能听懂自然语言指令、自主操控机械臂完成桌面任务的具身智能体。

这个智能体的核心特征是：**在正式执行任务之前，会先自主探索环境，积累对环境的理解，然后再基于探索经验执行任务。**

智能体由两个核心模型驱动：**MiniMax-M3 大模型作为大脑**（负责探索策略、任务规划、视觉感知、失败重规划），**VLA 模型作为脊髓**（负责将高层指令转化为机械臂的具体动作）。两者缺一不可，LLM 是决策核心而非辅助工具。

### 具体来说，智能体能做什么

用户输入一句自然语言指令，例如：
- "把红色方块放到蓝色区域里"
- "先把杯子推到左边，再把盘子翻过来"
- "把两个方块叠在一起"

智能体需要：
1. 先在环境中自由探索一段时间（动手"玩一玩"，摸清环境的物理特性）
2. 然后根据指令规划动作序列
3. 逐步操控机械臂完成任务
4. 如果中途失败了，能自己调整策略重试

---

## 二、为什么干这个项目

### 学术定位

本项目属于**具身智能（Embodied AI）**领域中的**视觉-语言-动作（VLA）**方向，是当前机器人研究的前沿热点。具体涉及：
- 仿真环境下的机器人操控
- 大模型驱动的机器人决策
- 探索式学习（Exploration-based Learning）

### 核心问题

现有的 VLA 模型（如 OpenVLA）虽然在通用数据上训练过，但面对一个**全新的具体环境**时，它不知道：
- 桌面有多大、边界在哪
- 物体有多重、推一下会滑多远
- 从什么角度抓取最稳
- 机械臂在这个仿真里的运动范围是什么

传统做法是直接让 VLA 硬猜，靠泛化能力碰运气。**本项目的核心假设是：如果让智能体先探索环境、积累经验，再执行任务，成功率会显著提升。**

### 与保研方向的匹配

- 研究意向"智能体与多模态感知"的直接落地
- 涉及 LLM Agent 工程化、多模态感知、机器人控制三大方向
- 有明确的创新点（探索模式），不是纯复现 demo

---

## 三、核心创新点：探索-执行双阶段机制

### 一句话概括

> 让智能体像人一样，先"熟悉一下环境"再干活，而不是上来就盲干。

### 探索阶段（Exploration Phase）

智能体在收到任务指令后、正式执行前，先进行一段自主探索。探索不是随机乱动，而是由 MiniMax-M3 大脑主导的有策略的探索：

- **物理特性探测**：推一下物体看它滑多远，试一下夹爪力度够不够
- **工作空间探测**：让机械臂往各个方向移动，摸清运动边界
- **交互策略探测**：尝试从不同角度接近同一物体，记录哪个角度成功率最高

探索过程中，MiniMax-M3 直接观察每一步操作后的环境截图（原生多模态能力），记录观测结果。得益于其 1M token 超长上下文，所有探索历史可以完整保留在上下文窗口中，不需要额外做记忆压缩。

探索结束后，MiniMax-M3 会总结出一份**环境知识摘要**，例如：
- "桌面有效区域约为 x∈[-0.3, 0.3], y∈[-0.2, 0.2]"
- "方块较轻，推力超过 0.05 会滑出桌面"
- "从顶部 45 度角抓取方块成功率最高"

### 执行阶段（Execution Phase）

MiniMax-M3 拿到任务指令 + 探索阶段的环境知识摘要，规划动作序列时**利用探索经验**指导 VLA 执行：
- 知道方块滑，就规划"低高度缓慢接近再夹"
- 知道边界在哪，就规划路径时避开超出范围的方向
- 知道哪个角度好抓，就优先选择该角度

执行过程中，MiniMax-M3 持续观察每步执行后的截图，判断子任务是否完成、是否需要重规划。

### 为什么这个创新点有价值

1. **有生物学依据**：人类面对陌生环境也会先摸索一下再操作
2. **有工程价值**：提升了 VLA 模型在新环境中的适应能力，无需重新训练
3. **可量化验证**：可以做对照实验，对比"有探索 vs 无探索"的任务成功率差异
4. **和已有经验结合**：探索策略借鉴了 e2e 测试中的探索性测试思想（fuzzing、边界值测试等），是技术栈的自然迁移

---

## 四、核心设计尝试：LLM 大脑 + VLA 脊髓的分层协作

### 这是一种什么尝试

传统 VLA 用法是端到端的：一张截图 + 一句指令 → 直接输出动作。VLA 既要"看懂环境"、又要"规划任务"、又要"输出动作"，但实际上它只擅长最后一步——把画面翻译成动作。它不会长程规划，不会主动探索，不会从失败中学习。

**本项目的核心设计尝试是：把"思考"和"反射"分开。**

- **LLM 大脑（MiniMax-M3）负责所有需要"思考"的事**：看懂环境在什么状态、决定先探索还是先执行、规划子任务序列、判断上一步成功了没有、失败了换什么策略
- **VLA 脊髓只负责"反射"**：收到大脑给的一条子指令（比如"靠近红色方块"），看一眼当前画面，输出机械臂下一步该怎么动

这种分层借鉴了生物神经系统：脊髓负责低级反射（手碰到烫的东西自动缩回），大脑负责高级决策（先看看周围环境再决定怎么操作）。VLA 像脊髓一样不需要理解全局任务，只需要对当前画面做出合理的局部动作反应。

### 这种分层要探索的问题

1. **LLM 的高层推理能否补偿 VLA 的泛化不足？** VLA 在陌生环境中动作质量会下降，如果 LLM 通过探索积累了环境知识，再指导 VLA 执行，能否弥补这个下降？
2. **探索经验如何从大脑传递给脊髓？** LLM 探索得到的知识是自然语言形式的环境摘要，这些知识需要通过"子指令"传递给 VLA。什么样的子指令表达方式能让 VLA 更好地利用？
3. **分层 vs 端到端的 trade-off**：引入 LLM 增加了一层推理延迟，但换来了探索能力和重规划能力。这个代价是否值得？在什么任务上值得、什么任务上不值得？

### 为什么选 MiniMax-M3 作为大脑

这种分层设计对大脑模型有特殊要求——它必须能直接看图（否则感知还得再过一层 VLM），必须能记住完整的探索历史（否则探索经验会丢失）。MiniMax-M3 的三个核心能力恰好满足：

| 能力 | 在分层架构中的作用 |
|------|--------------|
| 原生多模态 | 大脑直接看环境截图，做探索观测、任务完成度判断，不需要额外接 VLM 做感知翻译 |
| 1M token 超长上下文 | 探索阶段的所有观测记录完整保留在上下文中，不丢失任何探索经验 |
| 强 Agent 能力 | 工具调用（调 VLA 执行）、多步规划、失败重规划等 Agent 逻辑 |

### 分层架构

| 层级 | 角色 | 模型 | 职责 |
|------|------|------|------|
| 高层 | 大脑 | MiniMax-M3 + LangChain | 探索策略制定、视觉感知（直接看截图）、任务拆解、利用探索经验规划、失败重规划 |
| 低层 | 脊髓 | VLA 模型 | 看当前画面 → 输出机械臂的 7 维动作向量，反射式执行 |

**分工边界**：
- MiniMax-M3 负责"看懂环境 + 想清楚做什么"——它自己能看截图，直接判断环境状态、规划子任务、校验执行结果
- VLA 负责"把指令变成动作"——接收 MiniMax-M3 输出的子指令，转化为 7 维动作向量控制机械臂
- 两者形成闭环：MiniMax-M3 每次调 VLA 执行后，重新看截图判断结果，决定继续下一步还是重规划

---

## 五、技术栈选型

| 组件 | 选型 | 说明 |
|------|------|------|
| 大脑 LLM | MiniMax-M3 | 原生多模态 + 1M 上下文 + 强 Agent 能力，通过 API 调用 |
| Agent 框架 | LangChain | 任务拆解、工具调用、探索-执行流程编排 |
| 脊髓 VLA | OpenVLA-7B（阶段二）/ 轻量 VLA（阶段一） | 输出 7 维机械臂动作向量 |
| 仿真环境 | PyBullet + gym-panda | Franka 机械臂 + 桌面场景，Python 接口 |
| 开发语言 | Python | 全栈统一 |

---

## 六、部署策略

采用渐进式部署，先验证流程跑通，再上大模型做正式实验：

| 阶段 | 设备 | 大脑 LLM | VLA 模型 | 目标 |
|------|------|---------|---------|------|
| 阶段一 | M4 MacBook Air | MiniMax-M3 API | 轻量小 VLA 或 Mock | 跑通完整闭环，验证 Agent 逻辑和探索机制 |
| 阶段二 | 4060 笔记本 (CUDA) | MiniMax-M3 API | OpenVLA-7B 4bit 量化 | 跑正式实验，录制 demo |

说明：
- MiniMax-M3 通过 API 调用，两阶段共用，不需要本地部署 LLM
- VLA 调用通过统一接口抽象，切换模型时上层 Agent 代码不需要改动
- 阶段一在 M4 Air 上只需跑 PyBullet + 调 MiniMax-M3 API + Mock VLA，不需要 GPU

---

## 七、任务设计

### 演示任务（3-5 类）

| 任务类型 | 示例指令 | 考察点 |
|---------|---------|--------|
| 简单抓取放置 | "把红色方块放到蓝色标记区" | 基础抓取能力 |
| 多物体操作 | "把两个方块分别放到左右两侧" | 多步骤任务规划 |
| 精细操控 | "把方块叠到杯子上" | 探索经验对精细操作的帮助 |
| 工具使用 | "用推的方式把方块移到目标位置" | 探索物理特性后的策略选择 |
| 长流程任务 | "把三个物体按红黄蓝顺序排列" | 长程规划与中途重规划 |

### 量化指标

| 指标 | 说明 |
|------|------|
| 任务成功率 | 每类任务跑 20 次，统计成功率 |
| 平均执行步数 | 完成任务所需的 VLA 调用次数 |
| 探索收益 | 对比"有探索 vs 无探索"的成功率提升幅度 |
| 失败恢复率 | 执行失败后重规划并最终成功的比例 |

---

## 八、实验设计（Ablation）

| 实验组 | 探索阶段 | 是否利用探索经验 | 预期作用 |
|--------|---------|----------------|---------|
| 基线组 | 无 | 无 | 纯 VLA 泛化基线 |
| 实验组 | 有 | 有 | 验证探索-执行机制的整体效果 |
| 消融组 A | 有 | 无（探索了但不参考） | 验证提升来自"利用知识"而非"热身" |
| 消融组 B | 无（直接执行但多跑几步） | 无 | 控制总步数，排除"多走几步所以效果好"的干扰 |

---

## 九、项目目标

### 最低目标（必须达成）
- 跑通"探索 → 执行 → 反馈"完整闭环
- 在至少 3 类任务上跑出可运行的 demo
- 形成有探索 vs 无探索的对照实验数据

### 理想目标（加分项）
- 探索模式带来 15%+ 的成功率提升
- 支持长流程多步骤任务（5 步以上子任务）
- 形成可复现的实验报告，能清晰讲清创新点

### 交付物
1. 可运行的完整代码项目
2. 实验数据与对照实验结果
3. 项目文档（研究背景、方法、实验结果）
4. demo 演示视频
